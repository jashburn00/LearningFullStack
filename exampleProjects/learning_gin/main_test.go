package main

import (
	"bytes"
	"testing"
)

var b bytes.Buffer

func TestInitGinServer(t *testing.T) {

	// t.Run("InitGinServer should run without erroring", func(t *testing.T) {
	// 	InitGinServer(&b)
	//	//PROBLEM: InitGinServer blocks indefinitely
	// })

	// t.Run("InitGinServer should be accessible at port")
}
