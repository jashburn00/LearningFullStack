package main

import (
	"context"
	"fmt"
	"net/http"
	"time"
)

func Main() {
	fmt.Print("Hello World!")

	// req, err := http.NewRequestWithContext(context.WithTimeout(context.Background(), time.Second*3), "GET", )
}
